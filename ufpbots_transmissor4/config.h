#define MODE  4
#define CE  8
#define CSN 7

const uint64_t add1 = 0x0a0c0a0c0aLL;
int transmit_mode = 0;

void TransmitterConfiguration()
{
  //pinMode(MODE, INPUT);
  //transmit_mode = digitalRead(MODE);
}
